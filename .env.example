# Development/Staging environment
NODE_ENV=development

# Server
PORT=3000
HOST=0.0.0.0

# Database (use k8s service name or external URL)
POSTGRES_URL=postgres://app_user:${POSTGRES_PASSWORD}@postgres-service:5432/review_job

# Redis (use k8s service name or external URL)
REDIS_URL=redis://:${REDIS_PASSWORD}@0.0.0.0:6379

# External APIs
SKINPORT_API_URL=https://api.skinport.com/v1/items

# Logging
LOG_LEVEL=debug

# Loki (optional - for centralized logging)
LOKI_HOST=http://loki:3100
APP_NAME=review-job-dev
